# 生产环境 API（EAS build / release 使用）
EXPO_PUBLIC_API_URL=https://api.cocoai.chat

# 开发环境 API（expo start / __DEV__=true 时使用）
# 真机调试：填写开发机局域网 IP（ifconfig | grep "inet "），不能用 localhost
# 模拟器：iOS 可用 localhost，Android 用 10.0.2.2
EXPO_PUBLIC_API_URL_DEV=http://192.168.x.x:8000
