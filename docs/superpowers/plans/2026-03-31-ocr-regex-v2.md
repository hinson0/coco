# OCR 正则增强 v2 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 扩展 `extractReceiptInfo()` 支持餐厅和医院小票，并将金额匹配从「取第一次」改为「取最后一次」避免子合计误命中。

**Architecture:** 仅修改 `supabase/functions/record-ocr/index.ts` 的 `extractReceiptInfo()` 函数：新增 3 条金额 pattern（消费/应收/个人账户支付）、改用 `matchAll` 取最后一次匹配、补充 `YYYY-MM-DD` 日期格式支持。

**Tech Stack:** Deno Edge Function（正则）

---

## 涉及文件

- **修改** `supabase/functions/record-ocr/index.ts`（仅 `extractReceiptInfo()` 函数，约第 25-57 行）

---

## Task 1: 更新 extractReceiptInfo()

**Files:**
- Modify: `supabase/functions/record-ocr/index.ts:25-57`

### 验证用 OCR 样本

在开始之前，准备三段 OCR 文字用于贯穿验证：

**样本 A — 餐厅预打单：**
```
南昌餐馆三店
预打结账单(1)
餐桌:卡座02
时间:2026-03-22 19:34
品名 数量 价格 金额
1.香辣田螺 1.00 32.00 32.00
2.南昌辣椒炒肉 1.00 32.00 32.00
3.三鲜腐竹煲 1.00 28.00 28.00
4.清炒小白菜 1.00 16.00 16.00
5.餐位 4.00 1.00 4.00
6.米饭 1.00 5.00 5.00
消费:117.00
应收:117.00
签名:
欢迎您的光临!
```

**样本 B — 医院缴费单：**
```
抚州市立医院
缴费告知单
开票时间:2025-12-26 08:14:33
结付金额:25.74
静脉采血 1 5.27
一次性使用真空吃血管（集采）1 0.47
乙肝五项定性 1 20
个人账户支付:25.74
账户共济支付:0.00
医保统筹支付:0.00
个人支付金额:0.00
```

**样本 C — 超市多合计行（验证取最后一次）：**
```
凯升购物广场
商品1 小计:29.90
商品2 小计:7.96
合计:15件
163.92
正价商品:148.45 特价商品:15.47
折计:170.15
斤上:6.23
应付金额:163.92
业1付卡:163.92
2026.3.24 20:29:15
```

---

- [ ] **Step 1: 验证当前代码对样本 A/B 的失败行为**

运行以下 node 脚本，确认当前正则不匹配样本 A 和 B：

```bash
node -e "
const sampleA = '南昌餐馆三店\n消费:117.00\n应收:117.00';
const sampleB = '抚州市立医院\n个人账户支付:25.74\n账户共济支付:0.00';
const oldPatterns = [
  /应.{0,2}金额[：:]\s*([\d]+\.[\d]{2})/,
  /实.{0,2}付[：:]\s*([\d]+\.[\d]{2})/,
  /合计[：:]?\s*\d+[件个张]?\s*\n?([\d]+\.[\d]{2})/,
  /总计[：:]\s*([\d]+\.[\d]{2})/,
  /小计[：:]\s*([\d]+\.[\d]{2})/,
];
for (const text of [sampleA, sampleB]) {
  const matched = oldPatterns.some(p => p.test(text));
  console.log('匹配结果:', matched ? '命中（不应该）' : '未命中（正确，当前 bug）');
}
"
```

预期输出：
```
匹配结果: 未命中（正确，当前 bug）
匹配结果: 未命中（正确，当前 bug）
```

---

- [ ] **Step 2: 验证新 pattern 在 node 中正确匹配**

```bash
node -e "
const sampleA = '南昌餐馆三店\n预打结账单\n消费:117.00\n应收:117.00\n欢迎光临';
const sampleB = '抚州市立医院\n结付金额:25.74\n个人账户支付:25.74\n账户共济支付:0.00\n医保统筹支付:0.00';
const sampleC = '凯升购物广场\n商品1 小计:29.90\n商品2 小计:7.96\n合计:15件\n163.92\n应付金额:163.92\n2026.3.24';

const newPatterns = [
  /应.{0,2}金额[：:]\s*([\d]+\.[\d]{2})/,
  /实.{0,2}付[：:]\s*([\d]+\.[\d]{2})/,
  /个人账.{0,2}支付[：:]\s*([\d]+\.[\d]{2})/,
  /合计[：:]?\s*\d+[件个张]?\s*\n?([\d]+\.[\d]{2})/,
  /总计[：:]\s*([\d]+\.[\d]{2})/,
  /消费[：:]\s*([\d]+\.[\d]{2})/,
  /应收[：:]\s*([\d]+\.[\d]{2})/,
  /小计[：:]\s*([\d]+\.[\d]{2})/,
];

function extract(ocrText) {
  for (const pattern of newPatterns) {
    const matches = [...ocrText.matchAll(new RegExp(pattern.source, 'g'))];
    const match = matches.at(-1) ?? null;
    if (match) {
      const val = parseFloat(match[1]);
      if (val > 0) return val;
    }
  }
  return null;
}

console.log('样本A(餐厅):', extract(sampleA));   // 期望: 117
console.log('样本B(医院):', extract(sampleB));   // 期望: 25.74
console.log('样本C(超市):', extract(sampleC));   // 期望: 163.92（应付金额，非小计29.90）
"
```

预期输出：
```
样本A(餐厅): 117
样本B(医院): 25.74
样本C(超市): 163.92
```

如果输出不符，不要继续，先检查 pattern。

---

- [ ] **Step 3: 验证日期正则覆盖 YYYY-MM-DD**

```bash
node -e "
const dates = [
  '时间:2026-03-22 19:34',    // 餐厅
  '开票时间:2025-12-26 08:14:33', // 医院
  '2026.3.24 20:29:15',        // 超市（现有格式）
  '2026年3月24日',              // 中文格式
];
for (const text of dates) {
  const isoMatch = text.match(/(\d{4})-(\d{2})-(\d{2})/);
  const dotMatch = text.match(/(\d{4})[.年](\d{1,2})[.月](\d{1,2})/);
  const m = isoMatch ?? dotMatch;
  const date = m
    ? m[1] + '-' + m[2].padStart(2,'0') + '-' + m[3].padStart(2,'0') + 'T00:00:00Z'
    : null;
  console.log(text.slice(0, 20), '->', date);
}
"
```

预期输出：
```
时间:2026-03-22 19:34 -> 2026-03-22T00:00:00Z
开票时间:2025-12-26 08 -> 2025-12-26T00:00:00Z
2026.3.24 20:29:15    -> 2026-03-24T00:00:00Z
2026年3月24日          -> 2026-03-24T00:00:00Z
```

---

- [ ] **Step 4: 修改 extractReceiptInfo() 函数**

将 `supabase/functions/record-ocr/index.ts` 第 25-57 行的 `extractReceiptInfo()` 完整替换为：

```typescript
function extractReceiptInfo(ocrText: string): ReceiptInfo {
  // 总金额：多模式依次尝试，取最后一次匹配（避免子合计误命中）
  const amountPatterns = [
    /应.{0,2}金额[：:]\s*([\d]+\.[\d]{2})/,          // 超市：应付金额（兼容 OCR 错字）
    /实.{0,2}付[：:]\s*([\d]+\.[\d]{2})/,              // 超市：实付/实际付款
    /个人账.{0,2}支付[：:]\s*([\d]+\.[\d]{2})/,        // 医院：个人账户支付（扣医保后自付）
    /合计[：:]?\s*\d+[件个张]?\s*\n?([\d]+\.[\d]{2})/, // 超市：合计 N 件 + 金额
    /总计[：:]\s*([\d]+\.[\d]{2})/,                    // 通用
    /消费[：:]\s*([\d]+\.[\d]{2})/,                    // 餐厅预打单
    /应收[：:]\s*([\d]+\.[\d]{2})/,                    // 餐厅预打单
    /小计[：:]\s*([\d]+\.[\d]{2})/,                    // 单品小票
  ];

  let amount: number | null = null;
  for (const pattern of amountPatterns) {
    const matches = [...ocrText.matchAll(new RegExp(pattern.source, "g"))];
    const match = matches.at(-1) ?? null;
    if (match) {
      const val = parseFloat(match[1]);
      if (val > 0) { amount = val; break; }
    }
  }

  // 商户名：第一行有意义的文字（排除纯数字/条码行）
  const lines = ocrText.split("\n")
    .map((l: string) => l.trim())
    .filter((l: string) => l.length > 1 && !/^[\d\s\-:.]+$/.test(l));
  const merchant = lines[0] ?? null;

  // 日期：优先 YYYY-MM-DD（医院/餐厅），fallback YYYY.M.D 或 YYYY年M月D日
  const isoMatch = ocrText.match(/(\d{4})-(\d{2})-(\d{2})/);
  const dotMatch = ocrText.match(/(\d{4})[.年](\d{1,2})[.月](\d{1,2})/);
  const dateMatch = isoMatch ?? dotMatch;
  const date = dateMatch
    ? `${dateMatch[1]}-${dateMatch[2].padStart(2, "0")}-${dateMatch[3].padStart(2, "0")}T00:00:00Z`
    : null;

  return { amount, merchant, date };
}
```

---

- [ ] **Step 5: 部署 record-ocr**

```bash
npx supabase functions deploy record-ocr --no-verify-jwt
```

预期输出包含：`Deployed Edge Function record-ocr`

---

- [ ] **Step 6: 端到端验证（Supabase Dashboard）**

打开 Supabase Dashboard → Functions → record-ocr → Logs，分别拍以下小票验证：

**测试 1 — 餐厅预打单：**
- 预期日志：`[EXTRACT] 金额:117 商户:南昌餐馆三店 日期:2026-03-22T00:00:00Z`
- 预期 App：出现账单卡片，金额 117，备注「南昌餐馆三店」

**测试 2 — 超市多商品小票：**
- 预期日志：`[EXTRACT] 金额:163.92 商户:凯升购物广场 日期:2026-03-24T00:00:00Z`
- 确认金额不是 29.90（子合计）

**测试 3 — 医院缴费单（如有）：**
- 预期日志：`[EXTRACT] 金额:25.74 商户:抚州市立医院`

---

- [ ] **Step 7: Commit**

```bash
git add supabase/functions/record-ocr/index.ts
git commit -m "feat: record-ocr 正则增强 v2，支持餐厅/医院小票，金额取最后一次匹配"
```
