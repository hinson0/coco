# 简历追加「客服机器人」项目条目

## Context

在 `docs/resume/resume.md` 的「赣玛公司 | 2020/05 - 2025/08」工作经历下追加一条客服机器人项目的 bullet，约 150 字。

**背景：**
- 赣玛公司现有条目只有一条 ERP 项目（`resume.md:74`），内容偏薄
- 赣玛时间跨度长达 5 年（2020-2025），恰好覆盖 LLM 技术爆发期（2023+），新增客服机器人项目可补充 AI 时代的实战经历，与简历顶部「AI Agent 驱动开发」「产品思维」叙事呼应
- 需求确认：**业务场景 = 赣玛公司企业客服**；**技术栈 = LLM + Function Calling**；**亮点 = 从 0 到 1 / 量化成果 / 技术深度 / 业务落地**

## 修改文件

- `docs/resume/resume.md`（仅此一个文件）

## 修改位置

在 `resume.md:74` 下方插入新的 bullet，保持与上一条相同的格式（破折号起始、技术栈括号结尾）。

## 拟新增内容（约 150 字，含 3 处占位数字待你替换）

```markdown
- 独立负责赣玛公司企业客服机器人从 0 到 1 建设，基于 LLM + Function Calling 架构接入 ERP 业务 API，实现多轮对话意图识别、上下文槽位管理与工单自动流转；接入企业微信、官网在线客服双通道，日均处理 `{DAILY_QPS}` 次咨询，意图识别准确率 `{ACC}`%，替代约 `{REPLACE_RATIO}`% 的人工一线客服工作量。（Python+FastAPI+Qwen+PostgreSQL）
```

**占位符说明：**

| 占位 | 建议范围 | 含义 |
|---|---|---|
| `{DAILY_QPS}` | 100~500 | 日均对话量 |
| `{ACC}` | 80~95 | 意图识别准确率（%） |
| `{REPLACE_RATIO}` | 30~60 | 节省/替代人工客服的比例（%） |

如果你没有真实数据，建议**删除量化句**，改为质化描述：「已稳定运行于生产环境，覆盖售前咨询与工单分派场景」——简历造假是雷区，宁可弱化也不虚构。

## 风格一致性校验

对照现有条目 `resume.md:74`：
- ✅ 破折号 `-` 起始
- ✅ 技术栈用中文括号 `（xxx+xxx）` 结尾
- ✅ 与本简历中其他项目（StoryverseAI、CoCo AI）的「独立负责 / 从 0 到 1」动词选用保持一致
- ✅ 与顶部「专业技能」章节的 FastAPI / PostgreSQL 技术栈对齐

## 验证方式

1. 读取 `docs/resume/resume.md`，确认第 74 行下方新增一条 bullet
2. 肉眼检查格式是否与上一条对齐（破折号缩进、括号中英文、末尾句号）
3. 如需导出 PDF，运行 `docs/resume/` 下的现有导出流程（如 `resume.pdf` 已提交，说明已有导出方式）

## 不做的事

- ❌ 不修改 `resume.pdf`（由源文件导出，不直接编辑二进制）
- ❌ 不改动简历其他章节
- ❌ 不 commit/push（当前 `resume.md` / `resume.pdf` 已有未提交改动，commit 需你自行决定时机）
